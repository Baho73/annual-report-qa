# Разбор документа
PyMuPDF>=1.24        # текст, встроенные закладки, рендер страниц для vision
pdfplumber>=0.11     # таблицы по линиям разметки, второй проход

# Данные и конфигурация
PyYAML>=6.0          # набор вопросов с эталонами

# Демонстрация
streamlit>=1.30

# Тесты
pytest>=8.0

# Обращения к моделям идут через urllib из stdlib: requests не добавлен намеренно.
# pandas не понадобился — 868 записей обсчитываются чистым Python, зависимость не окупается.
# faiss-cpu и модель эмбеддингов понадобятся, когда будет реализована векторная ветка (T-012, отложена).
